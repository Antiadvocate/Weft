node_modules
dist
*.local
.DS_Store
tsconfig.tsbuildinfo
.claude/settings.local.json

# Save exports. These are personal play data — a player's characters, memories and history —
# and they are generated into the working tree by hand-built saves and by downloads. They are
# never part of the engine and never belong in the repository.
*.weft.json
*.weaver.json
